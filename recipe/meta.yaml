{% set name = "py-partiql-parser" %}
{% set version = "0.6.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/py_partiql_parser-{{ version }}.tar.gz
  sha256: 09cecf916ce6e3da2c050f0cb6106166de42c33d34a078ec2eb19377ea70389a

build:
  skip: true  # [py<37]
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python
    - hatchling
    - pip
  run:
    - python

test:
  imports:
    - py_partiql_parser
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/getmoto/py-partiql-parser/
  summary: Pure Python PartiQL Parser
  description: |
    A tokenizer/parser/executor for the PartiQL-language, in Python.
  dev_url: https://github.com/getmoto/py-partiql-parser/
  doc_url: https://github.com/getmoto/py-partiql-parser/
  license: MIT
  license_family: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - JamesRobertsonGames
